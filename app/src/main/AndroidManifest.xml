<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projects.com.br.myapplication" >

    <application
        android:name=".Manager.AplicationManager"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Trabalho Marcus"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".Controllers.Activitys.Controllers.MainActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".Controllers.Activitys.Controllers.FormularioActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".Controllers.Activitys.Controllers.DespesasActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
